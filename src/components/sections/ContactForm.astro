---
import ContactFormReact from "@/components/ContactFormReact.tsx";
import { getImage } from "astro:assets";

interface Props {
  title: string;
  subtitle: string;
  donationTitle: string;
  donationDescription: string;
  donationImage: string;
  donationImageAlt: string;
  donationButtonText: string;
  donationButtonUrl: string;
  newsletterTitle: string;
  newsletterDescription: string;
}

const props = Astro.props;

// Get the optimized image URL
const image = await getImage({
  src: props.donationImage,
  width: 800,
  height: 450,
  format: "webp",
});
---

<ContactFormReact {...props} donationImage={image.src} client:load />
